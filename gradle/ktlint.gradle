ktlint {
    version = "0.44.0"
    android = true
    enableExperimentalRules = false
    verbose = true
    disabledRules = [
            "indent",
            "import-ordering",
            "no-blank-line-before-rbrace",
    ]
}
